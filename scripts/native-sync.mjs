import fs from 'node:fs/promises';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const source=path.join(root,'native-web');const iosPublic=path.join(root,'ios','App','App','public');await fs.access(path.join(source,'index.html'));await fs.rm(iosPublic,{recursive:true,force:true});await fs.cp(source,iosPublic,{recursive:true});await fs.copyFile(path.join(root,'capacitor.config.json'),path.join(root,'ios','App','App','capacitor.config.json'));console.log('native:sync OK — native-web/ -> ios/App/App/public/');
